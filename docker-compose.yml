services:
  app:
    image: ${IMAGE:-docker-spring-demo}
    build:
      context: .
      dockerfile: Dockerfile
    container_name: docker-spring-demo
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=dev
    restart: unless-stopped