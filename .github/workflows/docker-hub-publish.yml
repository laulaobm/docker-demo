name: DockerHub Publish

on:
  push:
    branches: [ "main" ]
    tags: [ 'v*.*.*' ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        run: |
          # Wir definieren den Namen einmal hier
          REPO=${{ secrets.DOCKERHUB_USERNAME }}/docker-spring-demo
          
          # Build & Tag für 'latest'
          IMAGE=$REPO:latest docker compose build app
          docker push $REPO:latest
          
          # Wenn es ein Tag (v1.0.0 etc.) ist, auch diesen pushen
          if [[ "${{ github.ref }}" == refs/tags/* ]]; then
            TAG=${{ github.ref_name }}
            IMAGE=$REPO:$TAG docker compose build app
            docker push $REPO:$TAG
          fi